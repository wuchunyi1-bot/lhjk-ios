# Tasks: order-pay-amount-version

- [x] 1. 结算 / 详情解码 `amountVersion`、`expectedPayableAmount`
- [x] 2. `OrderService.orderPay` 改为 POST JSON body，带回两字段
- [x] 3. `PaymentService` / 确认订单支付传入契约字段；金额变化时提示并刷新
- [x] 4. 更新 OpenSpec 与 `docs/api-inventory.md`
