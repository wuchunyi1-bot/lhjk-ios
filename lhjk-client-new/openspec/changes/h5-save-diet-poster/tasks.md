# Tasks: h5-save-diet-poster

- [x] 1. OpenSpec：`h5-host` / `networking` delta，同步主 spec 与 `docs/api-inventory.md`
- [x] 2. `APIManager.getDataAsync`：已认证 GET，按二进制读响应
- [x] 3. `DiningSchemeService`：`downloadDietPoster` + 写入相册
- [x] 4. `FundeNativeBridge` 处理 `saveImageToAlbum`（成功/失败 Toast，权限拒绝引导设置）
