# Tasks: adapt-fundee-order-cancel

- [x] 1.1 编写 proposal / design / spec / tasks
- [x] 1.2 `OrderModels` 取消请求模型 + `OrderService.insertOrEditOrder`
- [x] 2.1 `OrderCancelRefundSheet` 退款原因弹层
- [x] 2.2 `OrderCancelFlow` 统一取消交互
- [x] 3.1 `OrderTabViewController` 列表取消接入
- [x] 3.2 `OrderDetailViewController` 详情取消接入
- [x] 3.3 `OrderListViewController` 监听刷新通知
