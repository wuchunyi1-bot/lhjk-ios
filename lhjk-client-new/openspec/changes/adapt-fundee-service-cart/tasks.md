## 1. 模型与服务

- [x] 1.1 `CartItem` / `CartLineDisplay` 模型；`CartService` 本地读写 + 原型种子
- [x] 1.2 `AppContainer` 注册 `cartService`

## 2. 购物车页

- [x] 2.1 `ServiceCartViewController` + ViewModel，对齐 `CartView.vue`
- [x] 2.2 路由 `/services/cart`；套餐详情「加入购物车」写入并跳转

## 3. 收尾

- [x] 3.1 标记 tasks 完成；提示新文件加入 Xcode
