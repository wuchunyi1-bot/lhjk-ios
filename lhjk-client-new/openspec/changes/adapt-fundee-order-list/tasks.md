## 1. Spec

- [x] 1.1 proposal / design / order-list delta spec

## 2. Tab

- [x] 2.1 `OrderListViewController` 改为 8 Tab（funde 顺序）与 query 映射
- [x] 2.2 `OrderTabViewController` 支持 `statusList`；退款/售后传 `6,9`；分 Tab 空态文案

## 3. Cell

- [x] 3.1 重写 `OrderCardCell`：机构+徽章 / 封面+名称卖点金额 / 操作按钮
- [x] 3.2 `AppOrderStatus`「待付款」→「待支付」；金额优先 payable
- [x] 3.3 操作按钮点击 Toast（真实 API 后续）
